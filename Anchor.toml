[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false
test = true

[programs.devnet]
solana_ai_defi_aggregator = "Y66qK5L367WXqaS3vsauu3z7V7ppQuL3Lj6FwuHKBPx"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 \"tests/**/*.ts\""

# Build with test-mode feature for tests
[test.build]
features = ["test-mode"]
